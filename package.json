{
  "name": "stackmate",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build": "npm run compile -ws",
    "release": "npm publish --workspaces --access public"
  },
  "directories": {
    "example": "examples"
  }
}
